import { useEffect, useRef, useState } from "react";
import { api } from "./api";
import { calculateCoverTransform } from "./framing";
import type { Card, Run, Workspace } from "./types";

const presets = { bust: { zoom: 1, offset_x: 0, offset_y: 0.02 }, tall: { zoom: 1.16, offset_x: 0, offset_y: -0.06 }, torso: { zoom: 1.28, offset_x: 0, offset_y: 0.08 } } as const;

export function CardGallery({ cards, onNavigate, onRefresh }: { cards: Card[]; onNavigate: (hash: string) => void; onRefresh: () => Promise<void> }) {
  const [includeDiscarded, setIncludeDiscarded] = useState(false);
  const [allCards, setAllCards] = useState(cards);
  useEffect(() => setAllCards(cards), [cards]);
  useEffect(() => { if (includeDiscarded) void api.listCards(true).then((body) => setAllCards(body.cards)).catch(() => setAllCards(cards)); else setAllCards(cards); }, [includeDiscarded, cards]);
  return <section className="gallery-view"><div className="page-heading"><div><p className="eyebrow">Saved explorations</p><h2>Card drafts</h2><p className="muted">Reopen a frame, trace it to its run, and keep the experiments that deserve another look.</p></div><label className="toggle"><input type="checkbox" checked={includeDiscarded} onChange={(event) => setIncludeDiscarded(event.target.checked)} /> Show discarded</label></div>{allCards.length === 0 ? <div className="empty-panel large-empty"><div className="empty-glyph">◇</div><h3>No card drafts yet</h3><p>Choose “Use this portrait” on a completed run sheet to open the direct framing workbench.</p><button className="button primary" onClick={() => onNavigate("#lab")}>Back to Style Lab</button></div> : <div className="gallery-grid">{allCards.map((card) => <article className="draft-card" key={card.card_id} onClick={() => onNavigate(`#card/${card.card_id}`)}><div className="draft-image"><img src={card.render_url} alt={card.label} /><span className={`decision-badge ${card.decision}`}>{card.decision}</span></div><div className="draft-meta"><strong>{card.label}</strong><span>{card.archetype} · zoom {card.framing.zoom.toFixed(2)}</span><small>Updated {new Date(card.updated_at).toLocaleDateString()}</small></div></article>)}</div>}<button className="button secondary refresh-gallery" onClick={() => onRefresh()}>Refresh drafts</button></section>;
}

export function CardWorkbench({ card: initialCard, onNavigate, onMessage, onRefresh }: { card: Card; workspace: Workspace; onNavigate: (hash: string) => void; onMessage: (message: string, kind?: "success" | "error") => void; onRefresh: () => Promise<void> }) {
  const [card, setCard] = useState(initialCard);
  const [frame, setFrame] = useState(initialCard.framing);
  const [saving, setSaving] = useState<"idle" | "saving" | "saved" | "error">("idle");
  const [showDetails, setShowDetails] = useState(false);
  const [sourceRun, setSourceRun] = useState<Run>();
  const drag = useRef<{ x: number; y: number; frame: typeof frame } | undefined>(undefined);
  const saveTimer = useRef<number | undefined>(undefined);
  useEffect(() => { setCard(initialCard); setFrame(initialCard.framing); }, [initialCard]);
  useEffect(() => { if (showDetails) void api.getRun(card.run_id).then((response) => setSourceRun(response.run)).catch(() => undefined); }, [showDetails, card.run_id]);
  useEffect(() => () => { if (saveTimer.current) window.clearTimeout(saveTimer.current); }, []);
  function scheduleSave(next: typeof frame) {
    setSaving("saving");
    if (saveTimer.current) window.clearTimeout(saveTimer.current);
    saveTimer.current = window.setTimeout(async () => { try { const response = await api.updateCard(card.card_id, { framing: next }); setCard(response.card); setFrame(response.card.framing); setSaving("saved"); } catch (error) { setSaving("error"); onMessage((error as Error).message, "error"); } }, 280);
  }
  function updateFrame(next: typeof frame, persist = true) { const clamped = { zoom: Math.max(1, Math.min(3, next.zoom)), offset_x: Math.max(-0.8, Math.min(0.8, next.offset_x)), offset_y: Math.max(-0.8, Math.min(0.8, next.offset_y)) }; setFrame(clamped); if (persist) scheduleSave(clamped); }
  function applyPreset(name: keyof typeof presets) { updateFrame(presets[name]); setCard((current) => ({ ...current, archetype: name })); }
  function nudge(axis: "offset_x" | "offset_y", amount: number) { updateFrame({ ...frame, [axis]: frame[axis] + amount }); }
  function onPointerDown(event: React.PointerEvent<HTMLDivElement>) { event.currentTarget.setPointerCapture(event.pointerId); drag.current = { x: event.clientX, y: event.clientY, frame }; }
  function onPointerMove(event: React.PointerEvent<HTMLDivElement>) { if (!drag.current) return; const bounds = event.currentTarget.getBoundingClientRect(); updateFrame({ ...drag.current.frame, offset_x: drag.current.frame.offset_x + (event.clientX - drag.current.x) / bounds.width, offset_y: drag.current.frame.offset_y + (event.clientY - drag.current.y) / bounds.height }); }
  function onPointerUp() { drag.current = undefined; scheduleSave(frame); }
  function onKeyDown(event: React.KeyboardEvent<HTMLDivElement>) { const amount = event.shiftKey ? 0.04 : event.altKey ? 0.002 : 0.012; if (event.key === "ArrowLeft") { event.preventDefault(); nudge("offset_x", -amount); } if (event.key === "ArrowRight") { event.preventDefault(); nudge("offset_x", amount); } if (event.key === "ArrowUp") { event.preventDefault(); nudge("offset_y", -amount); } if (event.key === "ArrowDown") { event.preventDefault(); nudge("offset_y", amount); } }
  async function editLabel(value: string) { setCard((current) => ({ ...current, label: value })); try { const response = await api.updateCard(card.card_id, { label: value }); setCard(response.card); setSaving("saved"); } catch (error) { onMessage((error as Error).message, "error"); } }
  async function decision(value: "keep" | "discard") { try { const response = await api.decideCard(card.card_id, value); setCard(response.card); onMessage(value === "keep" ? "Card kept in the draft gallery" : "Card marked discarded", "success"); } catch (error) { onMessage((error as Error).message, "error"); } }
  const transform = calculateCoverTransform(card.source_dimensions || [336, 276], [336, 276], frame);
  const imageStyle = { width: `${(transform.scaled_width / 336) * 100}%`, height: `${(transform.scaled_height / 276) * 100}%`, left: `${(transform.left / 336) * 100}%`, top: `${(transform.top / 276) * 100}%`, objectFit: "fill" as const };
  return <section className="card-workbench-view"><div className="workbench-top"><button className="back-link" onClick={() => onNavigate(`#run/${card.run_id}`)}>← Back to run</button><div><p className="eyebrow">Card Workbench</p><h2>Frame one result</h2><p className="muted">Drag or nudge the source inside the art window. The full-resolution run output remains the source.</p></div><button className="button secondary" onClick={() => setShowDetails((value) => !value)}>{showDetails ? "Hide details" : "Details"}</button></div><div className="card-workbench-layout"><div className="live-card-column"><div className="live-card-shell"><div className="live-card-heading">{card.label}</div><div className="live-art-window" tabIndex={0} role="application" aria-label="Portrait framing surface" onPointerDown={onPointerDown} onPointerMove={onPointerMove} onPointerUp={onPointerUp} onPointerCancel={onPointerUp} onKeyDown={onKeyDown}><img src={card.source_url} alt="Selected run output" style={imageStyle} /><span className="center-line horizontal" /><span className="center-line vertical" /></div><div className="live-card-copy"><strong>Experimental card-context preview</strong><span>336 × 276 art window</span></div></div><div className="save-state"><span className={`save-dot ${saving}`} />{saving === "saving" ? "Saving framing…" : saving === "error" ? "Could not save" : saving === "saved" ? "Saved" : "Ready"}</div><p className="keyboard-help">Focus the art window. Arrow keys nudge · Alt = fine · Shift = large.</p></div><aside className="frame-controls surface"><p className="eyebrow">Visible framing</p><h3>Choose a starting frame</h3><div className="preset-buttons">{(Object.keys(presets) as Array<keyof typeof presets>).map((name) => <button key={name} className={card.archetype === name ? "selected" : ""} onClick={() => applyPreset(name)}>{name[0].toUpperCase() + name.slice(1)}<small>{presets[name].zoom.toFixed(2)}× start</small></button>)}</div><label className="field-label zoom-field">Zoom <output>{frame.zoom.toFixed(2)}×</output><input type="range" min="1" max="3" step="0.01" value={frame.zoom} onChange={(event) => updateFrame({ ...frame, zoom: Number(event.target.value) })} /></label><div className="nudge-grid"><button onClick={() => nudge("offset_y", -0.012)}>↑</button><button onClick={() => nudge("offset_y", 0.012)}>↓</button><button onClick={() => nudge("offset_x", -0.012)}>←</button><button onClick={() => nudge("offset_x", 0.012)}>→</button></div><button className="button secondary full-width" onClick={() => applyPreset("bust")}>Reset to Bust</button><label className="field-label">Card label<input value={card.label} onChange={(event) => void editLabel(event.target.value)} /></label><div className="decision-actions"><button className={`button ${card.decision === "keep" ? "keep-active" : "primary"}`} onClick={() => decision("keep")}>Keep card</button><button className={`button ${card.decision === "discard" ? "discard-active" : "secondary"}`} onClick={() => decision("discard")}>Discard</button></div></aside></div>{showDetails && <CardDetails card={card} run={sourceRun} onNavigate={onNavigate} />}</section>;
}

function CardDetails({ card, run, onNavigate }: { card: Card; run?: Run; onNavigate: (hash: string) => void }) { return <aside className="details-panel surface"><div><p className="eyebrow">Traceable provenance</p><h3>Source and framing details</h3></div><dl><dt>Run</dt><dd><button className="text-button" onClick={() => onNavigate(`#run/${card.run_id}`)}>{run?.recipe_snapshot.name || card.run_id}</button></dd><dt>Run item</dt><dd>{card.run_item_id}</dd><dt>Model</dt><dd>{run?.model || "—"}</dd><dt>Reference pack</dt><dd>{run ? `${run.references_snapshot.length} ordered references` : "Loading…"}</dd><dt>Source dimensions</dt><dd>{card.source_dimensions?.join(" × ") || "—"}</dd><dt>Framing transform</dt><dd><code>{JSON.stringify(card.framing)}</code></dd><dt>Saved render</dt><dd>{card.render_dimensions?.join(" × ") || "—"}</dd></dl>{run && <div className="reference-thumbs">{run.references_snapshot.map((reference) => <img key={reference.id} src={reference.input_url} alt={reference.label} title={reference.label} />)}</div>}<p className="muted">The run snapshot contains the recipe and ordered reference pack used for this portrait.</p></aside>; }
