import { useEffect, useMemo, useState } from "react";
import { api } from "./api";
import type { Bootstrap, Framing, Model, PipelineStyle, ProductionBatch, ProductionItem, SearchResult, Source } from "./types";

type Notice = (message: string, kind?: "success" | "error") => void;
type Shared = { bootstrap: Bootstrap; navigate: (hash: string) => void; refresh: () => Promise<void>; notify: Notice };

function formatCost(value: number | null | undefined) { return value == null ? "Cost returned by provider" : value === 0 ? "$0 simulation" : `$${value.toFixed(4)}`; }
function selectedStyle(bootstrap: Bootstrap) { return bootstrap.style.active; }

export function SourcesView({ bootstrap, navigate, refresh, notify }: Shared) {
  const [selected, setSelected] = useState(bootstrap.selected_source_ids);
  const [query, setQuery] = useState("");
  const [results, setResults] = useState<SearchResult[]>([]);
  const [busy, setBusy] = useState("");
  const style = selectedStyle(bootstrap);
  const model = bootstrap.models.find((entry) => entry.id === style.generation.model_id && entry.execution_mode === style.generation.execution_mode);
  const activeBatch = bootstrap.batches.find((batch) => ["queued", "running", "processing"].includes(batch.status));
  useEffect(() => setSelected(bootstrap.selected_source_ids), [bootstrap.selected_source_ids.join(",")]);
  async function saveSelection(next: string[]) { setSelected(next); try { await api.updateSelection(next); } catch (error) { notify((error as Error).message, "error"); } }
  async function search() { if (!query.trim()) return; setBusy("search"); try { setResults((await api.searchSources(query)).results); } catch (error) { notify((error as Error).message, "error"); } finally { setBusy(""); } }
  async function importOne(result: SearchResult) { setBusy(`import-${result.pexels_photo_id}`); try { await api.importSources([result]); await refresh(); notify("Source image added"); } catch (error) { notify((error as Error).message, "error"); } finally { setBusy(""); } }
  async function upload(event: React.ChangeEvent<HTMLInputElement>) { const file = event.target.files?.[0]; if (!file) return; setBusy("upload"); try { await api.uploadSource(file, file.name.replace(/\.[^.]+$/, "")); await refresh(); notify("Source image uploaded"); } catch (error) { notify((error as Error).message, "error"); } finally { setBusy(""); event.target.value = ""; } }
  async function makeCards() { setBusy("make"); try { const response = await api.createProduction(selected, style.identity.style_version_id); notify("Card production started"); navigate(`#cards/${response.batch.batch_id}`); await refresh(); } catch (error) { notify((error as Error).message, "error"); } finally { setBusy(""); } }
  const problem = !selected.length ? "Select at least one source image." : activeBatch ? "A generation batch is already active." : !model?.available ? "The active model is unavailable." : (model.max_input_references < 1 + style.reference_pack.assets.filter((asset) => asset.role === "generation-reference").length ? "The active model cannot accept the style references." : "");
  return <section className="page sources-page">
    <div className="page-heading"><div><p className="eyebrow">Sources</p><h2>Choose the images that become cards</h2><p>Select and order portrait sources. The active house style will make one finished card per source.</p></div><span className="status-chip">{selected.length} selected</span></div>
    <section className="surface source-management"><div className="panel-heading"><div><p className="eyebrow">Add source images</p><h3>Project library</h3></div><label className="button secondary file-button">Upload image<input aria-label="Upload source image" type="file" accept="image/*" onChange={upload} /></label></div><form className="search-row" onSubmit={(event) => { event.preventDefault(); void search(); }}><input aria-label="Search source images" value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search Pexels portraits" /><button className="button secondary" disabled={busy === "search"}>{busy === "search" ? "Searching…" : "Search"}</button></form>{results.length > 0 && <div className="search-results">{results.map((result) => <article key={result.pexels_photo_id}><img src={result.preview_url || result.selected_image_url} alt={result.label || result.photographer || "Search result"} /><div><strong>{result.photographer || "Portrait source"}</strong><button className="button secondary" disabled={busy === `import-${result.pexels_photo_id}`} onClick={() => void importOne(result)}>Add source</button></div></article>)}</div>}</section>
    <section className="source-library">{bootstrap.sources.length === 0 ? <div className="surface empty-panel"><span className="empty-glyph">＋</span><h3>No source images yet</h3><p>Add an upload or search Pexels above. Nothing is sent to a generation provider until you make cards.</p></div> : <div className="source-grid">{bootstrap.sources.map((source) => { const order = selected.indexOf(source.id); return <article className={`surface source-tile ${order >= 0 ? "selected" : ""}`} key={source.id}><button className="source-select" aria-pressed={order >= 0} onClick={() => void saveSelection(order >= 0 ? selected.filter((id) => id !== source.id) : [...selected, source.id])}>{source.image_url && <img src={source.image_url} alt={source.label} />}<span>{source.label}</span>{order >= 0 && <b>#{order + 1}</b>}</button><button className="text-button danger" onClick={async () => { if (!window.confirm(`Delete ${source.label}?`)) return; try { await api.deleteSource(source.id); await refresh(); notify("Source removed"); } catch (error) { notify((error as Error).message, "error"); } }}>Remove</button></article>; })}</div>}</section>
    <section className="surface make-cards-panel"><div><p className="eyebrow">Active house style</p><h3>{style.identity.label}</h3><p>{selected.length} generation call{selected.length === 1 ? "" : "s"} · {style.generation.execution_mode} · {style.generation.model_id} · {formatCost(model?.pricing?.[0]?.cost_usd as number | undefined)}</p></div><button className="button primary large" disabled={Boolean(problem) || Boolean(busy) || Boolean(activeBatch)} onClick={() => void makeCards()}>{busy === "make" ? "Starting…" : `Make ${selected.length} card${selected.length === 1 ? "" : "s"} with ${style.identity.label}`}</button>{problem && <p className="validation-note" role="status">{problem}</p>}</section>
  </section>;
}

function latestItems(batch: ProductionBatch, selectedIds: string[]) { return selectedIds.map((sourceId) => batch.items?.filter((item) => item.source_id === sourceId).sort((a, b) => b.attempt_number - a.attempt_number)[0]).filter(Boolean) as ProductionItem[]; }

export function CardsView({ bootstrap, navigate, refresh, notify, batchId, cardId }: Shared & { batchId?: string; cardId?: string }) {
  const [batch, setBatch] = useState<ProductionBatch | undefined>();
  const [busy, setBusy] = useState("");
  const [framing, setFraming] = useState<Framing>({ zoom: 1, offset_x: 0, offset_y: 0 });
  const selectedBatch = bootstrap.batches.find((candidate) => candidate.batch_id === batchId) || bootstrap.batches.find((candidate) => candidate.purpose === "card-production");
  useEffect(() => { if (selectedBatch) void api.getProduction(selectedBatch.batch_id).then((response) => setBatch(response.batch)).catch((error) => notify(error.message, "error")); }, [selectedBatch?.batch_id]);
  useEffect(() => { if (!batch || !["queued", "running", "processing"].includes(batch.status)) return; const timer = window.setInterval(() => void api.getProduction(batch.batch_id).then((response) => setBatch(response.batch)).catch(() => undefined), 800); return () => window.clearInterval(timer); }, [batch?.batch_id, batch?.status]);
  const current = batch;
  const items = current ? latestItems(current, current.selected_source_ids) : [];
  const card = items.find((item) => item.item_id === cardId);
  async function refreshBatch() { if (!current) return; setBatch((await api.getProduction(current.batch_id)).batch); await refresh(); }
  async function approve(item: ProductionItem) { if (!current) return; setBusy(item.item_id); try { await api.approve(current.batch_id, item.item_id); notify("Card approved"); await refreshBatch(); } catch (error) { notify((error as Error).message, "error"); } finally { setBusy(""); } }
  async function tryAnother(item: ProductionItem) { if (!current || !window.confirm("Try another makes one additional paid generation attempt for this source. Continue?")) return; setBusy(item.item_id); try { await api.tryAnother(current.batch_id, item.source_id); notify("Another attempt queued"); await refreshBatch(); } catch (error) { notify((error as Error).message, "error"); } finally { setBusy(""); } }
  async function render(item: ProductionItem) { if (!current) return; setBusy(`render-${item.item_id}`); try { await api.renderFraming(current.batch_id, item.item_id, framing); notify("Framing saved without a generation call"); await refreshBatch(); } catch (error) { notify((error as Error).message, "error"); } finally { setBusy(""); } }
  async function bundle() { if (!current) return; setBusy("bundle"); try { const result = await api.bundle(current.batch_id); const link = document.createElement("a"); link.href = result.download_url; link.download = "approved-cards.zip"; link.click(); notify("Approved card bundle downloaded"); } catch (error) { notify((error as Error).message, "error"); } finally { setBusy(""); } }
  if (!current) return <section className="page cards-page"><div className="empty-panel surface">{selectedBatch ? <><span className="spinner" /><p>Loading card production…</p></> : <><h2>No card batch yet</h2><p>Select source images and make cards to see finished candidates here.</p><button className="button primary" onClick={() => navigate("#sources")}>Choose source images</button></>}</div></section>;
  const ready = current.progress.ready_cards; const approved = current.progress.approved_cards;
  return <section className="page cards-page"><div className="page-heading"><div><p className="eyebrow">Cards</p><h2>Review finished cards</h2><p>{current.style_version_id} · {formatCost(current.cost_usd)} · {current.progress.paid_calls} paid call{current.progress.paid_calls === 1 ? "" : "s"}</p></div><div className="heading-actions"><span className="status-chip">{ready} / {current.progress.selected_sources} ready</span>{current.status === "ready" && <button className="button primary" disabled={busy === "bundle" || approved !== current.progress.selected_sources} onClick={() => void bundle()}>Download approved cards</button>}</div></div><div className="batch-progress surface"><strong>{approved} / {current.progress.selected_sources} approved</strong><span className="progress-track"><i style={{ width: `${current.progress.selected_sources ? (ready / current.progress.selected_sources) * 100 : 0}%` }} /></span><small>{current.status === "ready" ? "Every source has a final card." : current.status === "ready-with-errors" ? "Some sources failed; ready cards remain reviewable." : `Production is ${current.status}.`}</small>{current.status === "ready-with-errors" && <button className="button secondary" onClick={async () => { setBusy("retry"); try { await api.retryFailed(current.batch_id); notify("Failed sources queued for retry"); await refreshBatch(); } catch (error) { notify((error as Error).message, "error"); } finally { setBusy(""); } }}>{busy === "retry" ? "Retrying…" : "Retry failed"}</button>}</div><div className="card-groups">{items.map((item) => <article className={`card-slot surface ${card?.item_id === item.item_id ? "focused" : ""}`} key={item.item_id}><header><div><p className="eyebrow">{item.source_label}</p><h3>Attempt {item.attempt_number}</h3></div><span className={`status-chip ${item.status}`}>{item.status}</span></header>{item.card_url ? <button className="card-image-button" onClick={() => navigate(`#cards/${current.batch_id}/${item.item_id}`)}><img className="final-card-image" src={item.card_url} alt={`${item.source_label} finished card`} /></button> : <div className="card-placeholder"><span className="spinner" />{item.error || item.status}</div>}<div className="card-actions">{item.status === "ready" && <><button className="button primary" disabled={busy === item.item_id} onClick={() => void approve(item)}>Approve card</button><button className="button secondary" disabled={Boolean(busy)} onClick={() => void tryAnother(item)}>Try another <small>paid</small></button><button className="button secondary" disabled={Boolean(busy)} onClick={() => { setFraming(item.framing || { zoom: 1, offset_x: 0, offset_y: 0 }); navigate(`#cards/${current.batch_id}/${item.item_id}`); }}>Adjust framing <small>no generation</small></button></>}{item.error && <button className="button secondary" onClick={async () => { try { await api.retryFailed(current.batch_id); await refreshBatch(); } catch (error) { notify((error as Error).message, "error"); } }}>Retry failed</button>}</div><details><summary>How this was made</summary><div className="details-content"><p>Final art: {item.art_url ? <a href={item.art_url}>336×276 PNG</a> : "—"} · master remains diagnostic.</p><p>Style checksum: <code>{current.style_checksum_sha256}</code></p><p>Render revision {item.render_revision} · card checksum <code>{item.card_checksum_sha256 || "—"}</code></p><p>References: {item.reference_stack.filter((reference) => reference.role === "generation-reference").length}; target examples are review-only.</p></div></details></article>)}</div>{card && <section className="surface framing-panel"><div className="panel-heading"><div><p className="eyebrow">Framing · {card.source_label}</p><h3>Adjust the existing master</h3><p>Saving this rerenders art and card deterministically. It makes zero generation calls.</p></div><button className="button secondary" onClick={() => navigate(`#cards/${current.batch_id}`)}>Close</button></div><img className="framing-preview" src={card.card_url} alt="Live framing preview" /><div className="framing-controls"><label>Zoom<input type="range" min="1" max="3" step="0.01" value={framing.zoom} onChange={(event) => setFraming({ ...framing, zoom: Number(event.target.value) })} /></label><label>Horizontal<input type="range" min="-1" max="1" step="0.01" value={framing.offset_x} onChange={(event) => setFraming({ ...framing, offset_x: Number(event.target.value) })} /></label><label>Vertical<input type="range" min="-1" max="1" step="0.01" value={framing.offset_y} onChange={(event) => setFraming({ ...framing, offset_y: Number(event.target.value) })} /></label></div><button className="button primary" disabled={busy === `render-${card.item_id}`} onClick={() => void render(card)}>{busy === `render-${card.item_id}` ? "Saving…" : "Save framing"}</button></section>}</section>;
}

export function StyleStudio({ bootstrap, navigate, refresh, notify }: Shared) {
  const [style, setStyle] = useState<PipelineStyle>(bootstrap.style.draft || bootstrap.style.active);
  const [cohort, setCohort] = useState<string[]>(bootstrap.selected_source_ids.slice(0, 3));
  const [trial, setTrial] = useState<ProductionBatch>();
  const [busy, setBusy] = useState("");
  const active = bootstrap.style.active; const draft = bootstrap.style.draft;
  useEffect(() => { if (draft) setStyle(draft); }, [draft?.identity.style_version_id, draft?.checksums.style_sha256]);
  const generationRefs = style.reference_pack.assets.filter((asset) => asset.role === "generation-reference"); const target = style.reference_pack.assets.find((asset) => asset.role === "target-example");
  async function beginEdit() { setBusy("draft"); try { const response = await api.createDraft(); setStyle(response.style); notify("Draft style ready"); } catch (error) { notify((error as Error).message, "error"); } finally { setBusy(""); } }
  async function patch(patchValue: Partial<PipelineStyle>) { try { const response = await api.updateDraft(patchValue); setStyle(response.style); await refresh(); } catch (error) { notify((error as Error).message, "error"); } }
  async function runTrial() { setBusy("trial"); try { const response = await api.runTrial(cohort); setTrial(response.batch); notify("Complete style trial started"); } catch (error) { notify((error as Error).message, "error"); } finally { setBusy(""); } }
  useEffect(() => { if (!trial || !["queued", "running", "processing"].includes(trial.status)) return; const timer = window.setInterval(() => void api.getProduction(trial.batch_id).then((response) => setTrial(response.batch)), 800); return () => window.clearInterval(timer); }, [trial?.batch_id, trial?.status]);
  async function activate() { if (!trial) return; setBusy("activate"); try { const response = await api.activateTrial(trial.batch_id); notify("New style version activated"); await refresh(); setStyle(response.style.active); setTrial(undefined); } catch (error) { notify((error as Error).message, "error"); } finally { setBusy(""); } }
  async function uploadReference(event: React.ChangeEvent<HTMLInputElement>) { const file = event.target.files?.[0]; if (!file) return; setBusy("reference"); try { const response = await api.uploadDraftReference(file); setStyle(response.style); await refresh(); notify("Generation reference added"); } catch (error) { notify((error as Error).message, "error"); } finally { setBusy(""); event.target.value = ""; } }
  async function updateReferences(next: PipelineStyle["reference_pack"]["assets"]) { await patch({ reference_pack: { ...style.reference_pack, assets: next } }); }
  return <section className="page style-page"><div className="page-heading"><div><p className="eyebrow">Style Studio</p><h2>{active.identity.label}</h2><p>Edit a draft, test the complete generation-to-card pipeline on a small cohort, then activate only a complete trial.</p></div><button className="button secondary" onClick={() => navigate("#sources")}>Back to Sources</button></div><section className="style-hero surface"><div><p className="eyebrow">Active version</p><h3>{active.identity.label}</h3><p><code>{active.identity.style_version_id}</code> · checksum <code>{active.checksums.style_sha256}</code></p></div><div className="palette-strip">{active.renderer.palette.map((colour) => <i key={colour} style={{ backgroundColor: colour }} title={colour} />)}</div></section><div className="reference-columns"><section className="surface"><div className="panel-heading"><h3>Generation references</h3><span>{generationRefs.length} / {style.generation.reference_limit - 1}</span></div><div className="reference-strip">{generationRefs.map((asset, index) => <figure key={asset.id}><img src={asset.image_url} alt={asset.label} /><figcaption>{asset.label}</figcaption>{draft && <div><button className="text-button" disabled={index === 0} onClick={() => void updateReferences(style.reference_pack.assets.map((candidate, position, all) => position === index - 1 ? all[index] : position === index ? all[index - 1] : candidate))}>Earlier</button><button className="text-button" disabled={index === generationRefs.length - 1} onClick={() => void updateReferences(style.reference_pack.assets.map((candidate, position, all) => position === index + 1 ? all[index] : position === index ? all[index + 1] : candidate))}>Later</button></div>}</figure>)}</div>{draft && <label className="button secondary file-button">Add generation reference<input type="file" accept="image/*" onChange={uploadReference} /></label>}<p className="muted">The identity source is sent first, followed by these references in order.</p></section><section className="surface target-example"><h3>Target example</h3>{target?.image_url && <img src={target.image_url} alt={target.label} />}<p><strong>Review only — never sent to the model.</strong></p></section></div>{!draft ? <section className="surface edit-callout"><h3>Keep the active version clear</h3><p>Production continues using {active.identity.label}. Create a separate draft before changing anything.</p><button className="button primary" disabled={busy === "draft"} onClick={() => void beginEdit()}>Edit as new version</button></section> : <><section className="surface editor"><div className="panel-heading"><div><p className="eyebrow">Draft</p><h3>What should change?</h3></div><span className="status-chip">{style.identity.style_version_id}</span></div><textarea aria-label="What should change?" value={style.generation.direction.intent} onChange={(event) => setStyle({ ...style, generation: { ...style.generation, direction: { ...style.generation.direction, intent: event.target.value } } })} onBlur={() => void patch({ generation: style.generation })} /><details open><summary>Generation</summary><div className="form-grid"><label>Model<input value={style.generation.model_id} onChange={(event) => setStyle({ ...style, generation: { ...style.generation, model_id: event.target.value } })} onBlur={() => void patch({ generation: style.generation })} /></label><label>Quality<select value={style.generation.quality} onChange={(event) => void patch({ generation: { ...style.generation, quality: event.target.value as PipelineStyle["generation"]["quality"] } })}><option>low</option><option>medium</option><option>high</option></select></label></div><label>Avoid<textarea value={style.generation.avoid} onChange={(event) => setStyle({ ...style, generation: { ...style.generation, avoid: event.target.value } })} onBlur={() => void patch({ generation: style.generation })} /></label></details><details><summary>Amiga processing</summary><div className="form-grid"><label>Dither strength<input type="number" min="0" max="1" step="0.01" value={style.renderer.dither.strength} onChange={(event) => setStyle({ ...style, renderer: { ...style.renderer, dither: { ...style.renderer.dither, strength: Number(event.target.value) } } })} onBlur={() => void patch({ renderer: style.renderer })} /></label><label>Edge threshold<input type="number" min="0" max="1" step="0.01" value={style.renderer.dither.edge_threshold} onChange={(event) => setStyle({ ...style, renderer: { ...style.renderer, dither: { ...style.renderer.dither, edge_threshold: Number(event.target.value) } } })} onBlur={() => void patch({ renderer: style.renderer })} /></label><label>Centering Y<input type="number" min="0" max="1" step="0.01" value={style.composition.centering[1]} onChange={(event) => setStyle({ ...style, composition: { ...style.composition, centering: [style.composition.centering[0], Number(event.target.value)] } })} onBlur={() => void patch({ composition: style.composition })} /></label></div></details><details><summary>Card</summary><p>Pixel-native {style.card_assembly.logical_card_size.join("×")} logical · {style.card_assembly.output_scale}× output · {style.card_assembly.driver_id}</p></details><div className="draft-summary"><strong>Draft checksum</strong> <code>{style.checksums.style_sha256}</code></div></section><section className="surface trial-panel"><div className="panel-heading"><div><p className="eyebrow">Calibration cohort</p><h3>Test complete style</h3></div><span>{cohort.length} / 3 sources</span></div><div className="cohort-picker">{bootstrap.sources.map((source) => <button key={source.id} className={cohort.includes(source.id) ? "selected" : ""} aria-pressed={cohort.includes(source.id)} onClick={() => setCohort((current) => current.includes(source.id) ? current.filter((id) => id !== source.id) : current.length < 3 ? [...current, source.id] : current)}>{source.image_url && <img src={source.image_url} alt={source.label} />}<span>{source.label}</span></button>)}</div><p>{cohort.length} paid call{cohort.length === 1 ? "" : "s"} · {formatCost(bootstrap.models.find((model) => model.id === style.generation.model_id)?.pricing?.[0]?.cost_usd as number | undefined)}. Masters are immediately processed into final cards.</p><button className="button primary" disabled={!cohort.length || Boolean(busy) || Boolean(trial)} onClick={() => void runTrial()}>{busy === "trial" ? "Starting…" : "Test complete style"}</button>{trial && <div className="trial-result"><span className={`status-chip ${trial.status}`}>{trial.status}</span>{trial.items.map((item) => <img key={item.item_id} src={item.card_url} alt={`${item.source_label} trial card`} />)}{trial.status === "ready" && <button className="button primary" disabled={busy === "activate"} onClick={() => void activate()}>{busy === "activate" ? "Activating…" : "Make this the active style"}</button>}</div>}</section></>}</section>;
}
